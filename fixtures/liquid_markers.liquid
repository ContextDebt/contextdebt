<h1>Our hacky summer sale</h1>
{% comment %} workaround until we upgrade the theme {% endcomment %}
<!-- HACK: remove after 2024-05-01 -->
<p>{{ product.title }} — we will delete this when the sale ends</p>
{%- comment -%}
  temporary fix for the cart drawer
{%- endcomment -%}
{% comment %} A date alone proves nothing: "# 2014-12-02 Add workaround" is an authored date {% endcomment %}
{% comment %} rejected match matters: `{{ kludge }} workaround until we upgrade` is a {% endcomment %}
{% comment %} Workaround for the "flush on exit" bug in upstream {% endcomment %}
{% comment %} "workaround until X" is the shape we look for {% endcomment %}
